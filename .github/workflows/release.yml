name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: New version in MAJOR.MINOR.PATCH format
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Check out main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -e '.[dev]'

      - name: Validate release version
        id: version
        env:
          VERSION: ${{ inputs.version }}
        run: |
          python scripts/version.py set "$VERSION"
          if git rev-parse "v${VERSION}" >/dev/null 2>&1; then
            echo "Tag v${VERSION} already exists" >&2
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Compile Python sources
        run: python -m compileall -q provider events main.py

      - name: Run tests
        run: python -m pytest -q

      - name: Install Dify Plugin CLI
        env:
          DIFY_CLI_VERSION: 0.2.0
        run: |
          curl --fail --location --silent --show-error \
            --output "$RUNNER_TEMP/dify" \
            "https://github.com/langgenius/dify-plugin-daemon/releases/download/${DIFY_CLI_VERSION}/dify-plugin-linux-amd64"
          chmod +x "$RUNNER_TEMP/dify"
          echo "$RUNNER_TEMP" >> "$GITHUB_PATH"

      - name: Package plugin
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          mkdir -p dist
          dify plugin package . -o "dist/wechat_work_trigger-v${VERSION}.difypkg"

      - name: Commit version and create tag
        env:
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add manifest.yaml pyproject.toml
          git commit -m "chore: release ${TAG}"
          git tag -a "$TAG" -m "$TAG"
          git push --atomic origin HEAD:main "refs/tags/${TAG}"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
        run: gh release create "$TAG" dist/*.difypkg --generate-notes --title "$TAG"
